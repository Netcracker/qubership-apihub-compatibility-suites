openapi: 3.1.0
info:
  title: Add type to union
  version: 1.0.0
paths:
  /example:
    get:
      parameters:
        - name: data
          in: query
          required: false
          schema:
            type: [string, integer, "null"]  # After - Union type with string, integer, and boolean
      responses:
        '200':
          description: OK
