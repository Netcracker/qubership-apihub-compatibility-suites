openapi: 3.1.0
info:
  title: Union to single type
  version: 1.0.0
paths:
  /path1:
    post:
      parameters:
        - name: input
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: OK
