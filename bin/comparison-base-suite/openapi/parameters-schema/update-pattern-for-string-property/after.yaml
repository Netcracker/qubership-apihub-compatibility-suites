openapi: 3.0.3
info:
  title: test
  version: 0.1.0
paths:
  /path1:
    post:
      parameters:
        - name: p1
          in: query
          schema:
            type: string
            pattern: '^\d{3}-\d{2}-\d{4}$'
      responses:
        '200':
          description: OK
          content: { }
