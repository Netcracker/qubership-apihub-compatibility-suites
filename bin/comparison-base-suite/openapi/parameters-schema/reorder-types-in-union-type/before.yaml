openapi: 3.1.0
info:
  title: Reorder types
  version: 1.0.0
paths:
  /path1:
    post:
      parameters:
        - name: flexible
          in: query
          required: false
          schema:
            type: ["null", string]
      responses:
        '200':
          description: OK
