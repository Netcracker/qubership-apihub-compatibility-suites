openapi: 3.1.0
info:
  title: Add type to union
  version: 1.0.0
paths:
  /example:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: [string, integer, "null"]
      responses:
        '200':
          description: OK
