openapi: 3.1.0
info:
  title: Remove Type from Union
  version: 1.0.0
paths:
  /example:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: [string, integer]  # Union of two types
      responses:
        '200':
          description: OK
