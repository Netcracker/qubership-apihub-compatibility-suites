openapi: 3.1.0
info:
  title: Change to union type
  version: 1.0.0
paths:
  /example:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: [string, integer]
      responses:
        '200':
          description: OK
