openapi: 3.1.0
info:
  title: Remove Type from Union
  version: 1.0.0
paths:
  /example:
    post:
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: [string, integer]
