openapi: 3.1.0
info:
  title: Add type to union
  version: 1.0.0
paths:
  /path1:
    post:
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: [string, integer, "null"]
